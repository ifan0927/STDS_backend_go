type: object
properties:
  refund_amount:
    type: integer
    minimum: 0
  deduction_amount:
    type: integer
    minimum: 0
  deposit_deduction_reason:
    type: string
    description: deduction_amount 大於 0 時必填（BR-10）
