type: object
required:
  - code
  - message
properties:
  code:
    type: string
    enum:
      - unpaid_bill
      - pending_meter
      - lease_not_active
      - deposit_not_held
      - charge_exceeds_deposit
      - checkout_date_before_lease_start
      - manual_rent_refund_decision_required
  message:
    type: string
  source_id:
    type: string
    nullable: true
